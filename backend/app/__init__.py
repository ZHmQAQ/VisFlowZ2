"""
VModule — PLC 视觉虚拟扩展模块

将工业相机和神经网络模型耦合到 PLC 控制的工业流水线上。
采用 PLC 软元件体系作为核心抽象，对 PLC 工程师零学习成本。

架构：
  ┌─────────────────────────────────────────┐
  │          VModule 虚拟软元件空间           │
  │                                         │
  │  外部 I/O     EX/EY (bit)  ED/EW (word) │  ← Modbus 映射到 PLC
  │  内部逻辑     VM (bit)     VD (word)     │  ← 控制相机/模型
  │  系统状态     SM (bit)     SD (word)     │  ← 只读诊断
  │  虚拟定时器   VT                         │
  │  虚拟计数器   VC                         │
  │                                         │
  │  扫描引擎: 读输入 → 执行逻辑 → 写输出    │
  └─────────────────────────────────────────┘

参考文档: 信捷 XG 系列 PLC 用户手册 [基本指令篇]
"""

__version__ = "0.1.0"
__product__ = "VModule"
__description__ = "PLC 视觉虚拟扩展模块"
